# 두두 로지스틱스 - 데이터 처리 정책서

- **작성자**: 김광진
- **작성일**: 2026-08-06
- **대상**: 접수 원장 `shipments` 3,045건 / 25칸

---

## 오늘 내는 것 세 가지

1. **정리 방식** - 어떤 기준으로 처리했는가
2. **오류 분류** - 어떤 예외가 나왔는가
3. **처리 방안** - 다음 단계에서 어떻게 할 것인가

셋 다 **칸 하나 안에** 들어갑니다. 이 문서는 칸 하나가 한 덩어리입니다.

정리만 하고 왜 그랬는지를 안 남기면 내일 일이 진행되지 않습니다. 검산할 때 "이걸 왜 했지"가 됩니다.

정답이 없습니다. 같은 칸을 봐도 무엇을 막을지는 사람마다 다릅니다. 조사한 칸이 세 개여도 됩니다.

---

## 조사 현황

내가 본 칸에 체크합니다. **안 본 칸도 정보입니다.** (체크 표기: `[V]`)

- [V] `tracking_no` - 운송장 번호
- [ ] `accepted_at` - 접수 시각
- [ ] `branch_code` - 지점 코드
- [ ] `branch_name` - 지점 이름
- [ ] `sender_id` - 보내는 사람 ID
- [ ] `sender_name` - 보내는 사람 이름
- [ ] `receiver_name` - 받는 사람 이름
- [V] `receiver_phone` - 받는 사람 전화
- [V] `receiver_area` - 받는 지역
- [ ] `receiver_dong` - 받는 동
- [V] `region_type` - 권역 구분
- [ ] `category` - 물품 분류
- [ ] `item_name` - 물품명
- [V] `weight_kg` - 무게
- [V] `width_cm` - 가로
- [V] `height_cm` - 세로
- [V] `depth_cm` - 높이
- [V] `volume_weight_kg` - 부피 무게
- [V] `billed_weight_kg` - 청구 무게
- [V] `size_grade` - 크기 등급
- [V] `price` - 요금
- [ ] `eta_date` - 도착 예정일
- [V] `status` - 배송 상태
- [V] `delivered_at` - 배송 완료 시각
- [ ] `channel` - 접수 경로

---

## 쓰기 전에 - 세 가지 눈금

칸을 채울 때 아래 셋을 골라 적습니다.

### 눈금 1. 처리 분류 - 이 예외를 무엇으로 했는가

- **수정** - 값을 고쳤다
- **삭제** - 행이나 값을 지웠다
- **통일** - 여러 표기를 하나로 묶었다
- **보류** - 손대지 않고 남겼다

### 눈금 2. 어디서 막는가 - 이 예외를 앞으로 어디서 걸러야 하는가

- **기계적 차단** - 화면이 애초에 못 넣게 한다. 필수 입력, 숫자만, 선택 목록
- **정책** - 규칙을 정해야 정해진다. 선택지를 어디까지 열지, 예외를 어디까지 허용할지
- **운영** - 사람이 받아야 한다. 상담원 연결, CS 접수, 환불 처리

세 층을 섞지 않습니다. 정책으로 정할 것을 기계로 막으면 현장이 못 씁니다. 기계로 막을 것을 사람에게 넘기면 같은 일이 계속 들어옵니다.

### 눈금 3. 확신도 - 내가 어디까지 정했는가

- **확정** - 이렇게 합니다
- **추천** - 이게 낫다고 보지만 최종 결정은 팀에서
- **선택지** - 두 갈래를 다 만들어 뒀습니다. 고르시면 됩니다

혼자 확정한 것보다 여럿이 같은 결론에 도달한 것이 믿을 만합니다. 혼자 판단하면 틀리기 쉽습니다.

---

# 칸별 정책

---

## 칸 - `tracking_no` (운송장 번호)

### 1. 초기 상태

- 값 종류: 3,000가지 (빈 칸 포함)
- 빈 칸: 0건
- 형식: 숫자 (int64)
- 어떻게 셌나: `tracking_no` 고유값 수 세기 (3,045행 중 3,000개 고유값)

### 2. 예외

**예외 1 - 완전히 동일한 이중 등록 중복 행**

- 몇 건: 45건 (총 90행)
- 어떻게 찾았나: `tracking_no` 중복 검색 후 25개 전체 컬럼 1:1 대조

### 3. 처리

**예외 1 - 이중 등록 중복 행**

- 분류: 삭제
- 왜 이렇게 정했나: 25개 전체 컬럼 값이 100% 동일한 완전 중복 행이므로 1건만 남기고 제거하는 것이 타당함
- 왜 생겼다고 보는가: 시스템 접수 전송 시 네트워크 재시도 또는 서버 이중 저장 오류
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1 - 이중 등록 중복 행**

- 층: 기계적 차단
- 어떻게: DB 테이블의 `tracking_no` 컬럼에 PRIMARY KEY 및 UNIQUE 제약조건 설정, 접수 API 멱등성 보장

### 5. 못 정한 것 - 누구에게 물어야 하나

- 없음

---

## 칸 - `status` (배송 상태)

### 1. 초기 상태

- 값 종류: 16가지
- 빈 칸: 0건
- 형식: 글자
- 어떻게 셌나: `status` 컬럼 값 분포(`value_counts()`) 확인

### 2. 예외

**예외 1 - 띄어쓰기 오탈자 (`배송 완료`, `미 배송`)**

- 몇 건: 24건 (`배송 완료` 10건, `미 배송` 14건)
- 어떻게 찾았나: 값 분포 상위 표기 필터링

**예외 2 - 유사 용어 표기 파편화 (`배달완료`, `완료`, `미배달`, `배송실패`, `반송`, `회수`, `배송중`, `배달중`)**

- 몇 건: 104건 (`배달완료` 8건, `완료` 6건, `미배달` 8건, `배송실패` 14건, `반송` 21건, `회수` 13건, `배송중` 18건, `배달중` 16건)
- 어떻게 찾았나: 규정 문서의 7개 표준 상태 목록과 대조

### 3. 처리

**예외 1 - 띄어쓰기 오탈자**

- 분류: 통일
- 왜 이렇게 정했나: 표준 표기와 맞추어 집계 오차 방지
- 왜 생겼다고 보는가: 현장 수동 입력 시 오기입
- 확신도: 확정

**예외 2 - 유사 용어 파편화**

- 분류: 통일
- 왜 이렇게 정했나: 규정 문서의 7개 표준 상태(집화처리, 간선상차, 간선하차, 배송출발, 배송완료, 미배송, 반품)로 매핑
- 왜 생겼다고 보는가: 지점/기사별 용어 혼용
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1, 2 - 오탈자 및 파편화**

- 층: 기계적 차단
- 어떻게: 텍스트 수동 입력을 금지하고 Enum 코드 및 드롭다운 선택 목록으로 제한

### 5. 못 정한 것 - 누구에게 물어야 하나

- `배송실패`, `미배달`을 `미배송` 하나의 상태로 통합할지 세부 사유 코드로 분리할지 -> 현장 운영 담당자

---

## 칸 - `weight_kg` (무게)

### 1. 초기 상태

- 값 종류: 181가지
- 빈 칸: 0건
- 형식: 섞임 (숫자 + 글자)
- 어떻게 셌나: 문자열 'kg' 포함 여부 및 수치 변환 후 값 분포 확인

### 2. 예외

**예외 1 - 단위 텍스트 혼입 (`11.6kg` 등)**

- 몇 건: 51건
- 어떻게 찾았나: `'kg'` 문자열 검색

**예외 2 - 음수 및 0 이하 수치 (`-1`, `0` 등)**

- 몇 건: 37건 (음수 20건, 0인 값 17건)
- 어떻게 찾았나: 수치 변환 후 `<= 0` 조건 필터링

**예외 3 - 극단적 이상치 (`999` 등)**

- 몇 건: 11건
- 어떻게 찾았나: 수치 변환 후 `> 100kg` 조건 필터링

### 3. 처리

**예외 1 - 단위 텍스트 혼입**

- 분류: 수정
- 왜 이렇게 정했나: 수치 연산 및 집계를 위해 'kg' 제거 후 float로 변환
- 왜 생겼다고 보는가: 입력 양식에 단위까지 수동 입력함
- 확신도: 확정

**예외 2 - 음수 및 0 이하 수치**

- 분류: 보류
- 왜 이렇게 정했나: 임의로 수치를 채우면 요금 검증이 왜곡되므로 원본을 보존(`raw_weight_kg`)하고 정제 컬럼은 결측(`NaN`)으로 격리
- 왜 생겼다고 보는가: 미측정건을 -1이나 0으로 임의 입력
- 확신도: 선택지 (선택지 1: NaN 격리 / 선택지 2: 화물 카테고리별 평균값 보정 / 선택지 3: 해당 행 삭제)

**예외 3 - 극단적 이상치**

- 분류: 보류
- 왜 이렇게 정했나: 999kg 등 오타 데이터는 재측정 전까지 `NaN`으로 격리
- 왜 생겼다고 보는가: 키보드 입력 오타
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1 - 단위 텍스트**

- 층: 기계적 차단
- 어떻게: number 타입 제어 및 단위 UI Label 처리

**예외 2, 3 - 음수, 0 및 이상치**

- 층: 기계적 차단
- 어떻게: 입력 범위(0.01kg ~ 100kg) 실시간 검증(Validation)

### 5. 못 정한 것 - 누구에게 물어야 하나

- 음수/0/이상치 수치가 들어간 건의 실제 무게 측정값 -> 접수 지점 담당자

---

## 칸 - `width_cm` / `height_cm` / `depth_cm` (가로 / 세로 / 높이)

### 1. 초기 상태

- 값 종류: `width_cm` 85가지, `height_cm` 51가지, `depth_cm` 49가지
- 빈 칸: 0건
- 형식: 섞임 (숫자 + 글자)
- 어떻게 셌나: 문자열 'cm' 포함 여부 및 수치 변환 후 분포 확인

### 2. 예외

**예외 1 - 단위 텍스트 혼입 (`32cm` 등)**

- 몇 건: 가로 46건
- 어떻게 찾았나: `'cm'` 문자열 검색

**예외 2 - 음수, 0 및 극단 이상치 (`-10`, `0`, `9999` 등)**

- 몇 건: 가로 17건 (음수 5, 0 4, 이상치 8), 세로 24건 (음수 5, 0 9, 이상치 10), 높이 15건 (음수 3, 0 4, 이상치 8)
- 어떻게 찾았나: 수치 변환 후 `<= 0` 또는 `> 200cm` 조건 필터링

### 3. 처리

**예외 1 - 단위 텍스트 혼입**

- 분류: 수정
- 왜 이렇게 정했나: 부피 무게 계산을 위해 'cm' 제거 후 수치 변환
- 확신도: 확정

**예외 2 - 음수, 0 및 극단 이상치**

- 분류: 보류
- 왜 이렇게 정했나: 오타 수치를 임의 보정하면 부피 무게 계산이 왜곡되므로 `NaN`으로 격리
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1, 2 - 단위 텍스트 및 비정상 수치**

- 층: 기계적 차단
- 어떻게: `number` 타입 지정 및 `1cm ~ 200cm` 범위 제한 설정

### 5. 못 정한 것 - 누구에게 물어야 하나

- 이상치(예: 9999cm) 화물의 실제 재측정 치수 -> 접수 지점

---

## 칸 - `volume_weight_kg` / `billed_weight_kg` (부피 무게 / 청구 무게)

### 1. 초기 상태

- 값 종류: `volume_weight_kg` 168가지, `billed_weight_kg` 189가지
- 빈 칸: `volume_weight_kg` 87건, `billed_weight_kg` 0건
- 형식: 숫자 (일부 'kg' 포함 2건)
- 어떻게 셌나: 규정 공식 연산값과 비교

### 2. 예외

**예외 1 - 부피 무게 결측 및 수식 계산 오차**

- 몇 건: 결측 87건, 계산 오차 55건
- 어떻게 찾았나: (가로 * 세로 * 높이) / 6000 연산값과 비교

**예외 2 - 청구 무게 수식 불일치 (부피 무게가 더 큼에도 실제 무게로 잘못 청구)**

- 몇 건: 137건
- 어떻게 찾았나: max(실제무게, 부피무게) 연산값과 비교

### 3. 처리

**예외 1, 2 - 수식 불일치 및 결측**

- 분류: 수정
- 왜 이렇게 정했나: 부피무게와 청구무게는 치수와 무게에 종속된 파생값이므로 규정 공식으로 일괄 자동 재산출
- 왜 생겼다고 보는가: 공식 미숙지 또는 수동 입력 오류
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1, 2 - 수식 불일치**

- 층: 기계적 차단
- 어떻게: 수동 입력을 금지하고(`readOnly`), 치수/무게 입력 시 서버/DB에서 자동 계산(Generated Column)

### 5. 못 정한 것 - 누구에게 물어야 하나

- 잘못 청구된 요금 차액의 소급 정산 여부 -> 정산/재무 담당자

---

## 칸 - `size_grade` (크기 등급)

### 1. 초기 상태

- 값 종류: 11가지 (빈 칸 포함)
- 빈 칸: 59건
- 형식: 글자
- 어떻게 셌나: `size_grade` 분포 및 규정 4개 등급 대조

### 2. 예외

**예외 1 - 비표준 축약 표기 및 규격 외 표기 (`극소`, `소`, `중`, `대`, `규격외`, `초과`)**

- 몇 건: 40건 (`극소` 10, `소` 6, `중` 10, `대` 7, `규격외` 2, `초과` 5)
- 어떻게 찾았나: 값 분포 필터링

**예외 2 - 등급 누락 (빈 칸)**

- 몇 건: 59건
- 어떻게 찾았나: `isna()` 필터링

### 3. 처리

**예외 1, 2 - 비표준 표기 및 결측**

- 분류: 통일
- 왜 이렇게 정했나: 크기 등급은 요금 구간 판정용이므로, 정제된 청구 무게 기준 규정 4개 등급(`극소형`, `소형`, `중형`, `대형`)으로 일괄 재부여
- 왜 생겼다고 보는가: 현장 수동 지정 및 입력 누락
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1, 2 - 등급 수동 지정 오차**

- 층: 기계적 차단
- 어떻게: 화면에서 크기 등급 선택 필드를 아예 제거하고, 청구 무게에 따라 백엔드 룰 엔진이 자동 부여

### 5. 못 정한 것 - 누구에게 물어야 하나

- 25kg 초과 화물 발생 시 별도 '규격외' 등급을 신설할지 여부 -> 영업/운영 기획팀

---

## 칸 - `price` (요금)

### 1. 초기 상태

- 값 종류: 17가지 (빈 칸 포함)
- 빈 칸: 215건
- 형식: 숫자
- 어떻게 셌나: 필터 및 결측치 확인

### 2. 예외

**예외 1 - 빈 칸**

- 몇 건: 215건
- 어떻게 찾았나: `isna()` 필터링

**예외 2 - 음수 요금 (`-1000`, `-1500`)**

- 몇 건: 4건
- 어떻게 찾았나: `< 0` 조건 필터링

### 3. 처리

**예외 1 - 빈 칸 215건**

- 분류: 보류
- 왜 이렇게 정했나: 안 받은 돈인지 미기재건인지 확인하기 전까지 0으로 채우지 않고 보류
- 왜 생겼다고 보는가: 접수 담당자의 기록 누락
- 확신도: 확정

**예외 2 - 음수 4건**

- 분류: 보류
- 왜 이렇게 정했나: 지우면 정산 흔적이 사라지므로 수정 없이 남김
- 왜 생겼다고 보는가: 환불/할인을 요금 칸에 마이너스로 입력함
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1 - 빈 칸**

- 층: 기계적 차단
- 어떻게: 요금은 사람이 입력하지 않고 무게/지역으로 자동 계산하며, 미계산 시 저장 차단

**예외 2 - 음수**

- 층: 정책
- 어떻게: 환불을 요금 칸에 적을지 별도 환불 항목을 둘지 정해야 함

### 5. 못 정한 것 - 누구에게 물어야 하나

- 빈 215건이 미수금인지 기록 누락인지 -> 접수 담당자
- 환불/할인 표기 방식 -> 정산 담당자

---

## 칸 - `delivered_at` (배송 완료 시각)

### 1. 초기 상태

- 값 종류: 2,752가지 (빈 칸 포함)
- 빈 칸: 293건
- 형식: 날짜/시간 (문자열)
- 어떻게 셌나: `status` 컬럼과 타임스탬프 결합 교차 필터링

### 2. 예외

**예외 1 - 배송 미완료 상태인데 완료 시각이 존재함**

- 몇 건: 94건 (미배송, 반품, 배송출발 상태이나 completed_at 기재)
- 어떻게 찾았나: `status != '배송완료'` & `delivered_at.notna()` 조건 검색

**예외 2 - 배송 완료 상태인데 완료 시각이 누락됨**

- 몇 건: 65건
- 어떻게 찾았나: `status == '배송완료'` & `delivered_at.isna()` 조건 검색

**예외 3 - 접수 시각보다 완료 시각이 빠른 시점 역전**

- 몇 건: 46건
- 어떻게 찾았나: `delivered_at < accepted_at` 파싱 대조

### 3. 처리

**예외 1 - 미완료 상태의 완료 시각**

- 분류: 삭제
- 왜 이렇게 정했나: 미완료 건에 완료 시각이 입력되어 있으면 완료 건으로 오집계되므로 `delivered_at` 값을 제거(`NaN`)
- 확신도: 확정

**예외 2 - 완료 상태의 완료 시각 누락**

- 분류: 보류
- 왜 이렇게 정했나: 시각을 추측하여 채울 수 없으므로 보류하고 감사 데이터로 격리
- 확신도: 추천

**예외 3 - 시점 역전**

- 분류: 보류
- 왜 이렇게 정했나: 타임스탬프 입력 오타건이므로 `delivered_at`을 비우고 오류 로그 저장
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1, 2, 3 - 완료 시각 오차 및 역전**

- 층: 기계적 차단
- 어떻게: `delivered_at` 수동 입력을 금지하고, 기사 앱에서 배송완료 스캔 시 서버 시각(`now()`)이 자동 입력되도록 구현

### 5. 못 정한 것 - 누구에게 물어야 하나

- 시점 역전 46건의 실제 배송 완료 시각 확인 -> 해당 지점 택배 기사 / 배송 시스템 담당자

---

## 칸 - `receiver_area` / `region_type` (받는 지역 / 권역 구분)

### 1. 초기 상태

- 값 종류: `receiver_area` 28가지, `region_type` 3가지
- 빈 칸: 0건
- 형식: 글자
- 어떻게 셌나: 도착 지역과 권역 구분 교차표(`crosstab`) 검증

### 2. 예외

**예외 1 - 내륙 지역인데 '제주' 권역으로 오입력**

- 몇 건: 20건 (서울, 경기, 부산, 대구 등 내륙 수령지)
- 어떻게 찾았나: `receiver_area != '제주'` & `region_type == '제주'` 조건 필터링

**예외 2 - 제주 지역인데 '일반' 권역으로 오입력**

- 몇 건: 1건
- 어떻게 찾았나: `receiver_area == '제주'` & `region_type != '제주'` 조건 필터링

### 3. 처리

**예외 1, 2 - 권역 오매핑**

- 분류: 수정
- 왜 이렇게 정했나: 권역은 부가 요금 기준이므로, 도착지 주소 마스터를 기준으로 `region_type`을 자동 재매핑
- 확신도: 확정

### 4. 앞으로 어디서 막나

**예외 1, 2 - 권역 오매핑**

- 층: 기계적 차단
- 어떻게: 권역 수동 선택 필드를 없애고, 주소 입력 시 우편번호/지번 마스터 테이블에 따라 자동 매핑되도록 구현

### 5. 못 정한 것 - 누구에게 물어야 하나

- 도서산간 전용 우편번호 DB 마스터 테이블 확보 -> 영업기획 / 물류 네트워크 담당자

---

## 칸 - `receiver_phone` (받는 사람 전화)

### 1. 초기 상태

- 값 종류: 2,780가지 (빈 칸 포함)
- 빈 칸: 265건
- 형식: 섞임 (숫자, 하이픈, 텍스트)
- 어떻게 셌나: 전화번호 정규식(`^010-\d{4}-\d{4}$`) 필터링

### 2. 예외

**예외 1 - 맨 앞 '0' 누락 (`103552703` 등 9~10자리)**

- 몇 건: 345건
- 어떻게 찾았나: 문자열 길이가 10자리이고 '10'으로 시작하는 패턴 검색

**예외 2 - 번호 결측 및 형태 불능 난수**

- 몇 건: 265건 (결측)
- 어떻게 찾았나: `isna()` 필터링

### 3. 처리

**예외 1 - 맨 앞 '0' 누락**

- 분류: 수정
- 왜 이렇게 정했나: 엑셀 숫자 변환 과정에서 맨 앞 '0'이 지워진 것이 명확하므로 `010-XXXX-XXXX` 형태로 복원
- 확신도: 확정

**예외 2 - 번호 결측**

- 분류: 보류
- 왜 이렇게 정했나: 없는 번호를 만들어 낼 수 없으므로 보류
- 확신도: 추천

### 4. 앞으로 어디서 막나

**예외 1, 2 - 전화번호 포맷 오류 및 누락**

- 층: 기계적 차단
- 어떻게: 프론트엔드 입력 필드에 정규표현식 검증 적용 및 Auto-hyphening 적용

### 5. 못 정한 것 - 누구에게 물어야 하나

- 수취인 연락처 누락 건 발생 시 대체 연락처 확보 절차 -> CS 운영팀

---

# 마무리

## 검산

**운송장 중복 제거 및 수치/비즈니스 정제 검산**

- 처리 전: 3,045행 (중복 45쌍, 상태 파편화 128건, 수치 단위/이상치 100여 건, 요금무게 수식 오차 137건, 권역 오매핑 21건, 전화번호 오류 610건 등)
- 처리 후: 3,000행 (상태 7종 표준화, `raw_` 원본 보존 컬럼 7개 신설, 수치 이상치 `NaN` 격리 및 부피/청구 무게 재산출 완료)
- 예상과 달랐던 것: 부피 무게가 더 큼에도 실제 무게로 요금을 잘못 청구한 사례가 137건이나 확인됨

## 동료와 대 보기

**주요 항목 판단 대조**
- 나의 판단: 수치 이상치(`-1kg`, `9999cm` 등)는 원본 보존 후 `NaN`으로 격리
- 다른 사람 판단: 해당 행 전체를 삭제하거나 평균값으로 대체
- 결론: 데이터 유실 방지 및 정확성을 위해 원본 컬럼(`raw_`) 보존 + 정제 컬럼 `NaN` 격리 방식으로 확정

## 원본 보존

- [V] 원래 칸은 그대로 두고 새 칸을 옆에 만들었다
- [ ] 원래 칸을 덮어썼다

새로 만든 칸:
- `raw_weight_kg`, `raw_width_cm`, `raw_height_cm`, `raw_depth_cm`, `raw_volume_weight_kg`, `raw_billed_weight_kg`, `raw_price`

원래 칸을 덮어썼다면 되돌릴 방법:
- 원본 로데이터 파일(`shipments - 김광진.csv`) 보관 및 `raw_` 컬럼 참조

## 이번에 다루지 않기로 한 것

- `price` 결측 215건 및 음수 4건의 강제 보정 (정산/접수 담당자 확인 전까지 보류)

## 다음 사람에게

- `shipments_fully_cleaned.csv` 파일은 1~8단계 정제 및 비즈니스 로직 보정이 모두 반영된 최신 파일입니다.
- 원본 값과 정제 값의 차이는 `raw_` 접두사가 붙은 컬럼과 대조하시면 바로 확인 가능합니다.

## 막힌 자리

- 없음