# 데이터 정제 및 신규 주문 시스템 설계 종합 보고서

> 본 보고서는 기존 원장(`shipments.csv`)의 데이터 결함 분석부터 **[1. 처리 방침]**, **[2. 검산결과]**, **[3. 남기기(배포 및 기록)]**까지의 전체 작업을 통합 정리한 문서입니다.

---

## 1. 처리 방침 (못 믿는 값을 어떻게 할까)

> **목적:** 데이터 결함의 근거를 세우고, 신규 주문 시스템에서 동일 오류가 재발하지 않도록 가드레일 규칙/기능을 매핑합니다.

### 1) 운송장 번호(`tracking_no`) 중복
* **못 믿는 값:** 전체 25개 컬럼이 100% 일치하는 이중 등록 데이터 **45쌍 (총 90행)** 발생
* **처리 방침:** `drop_duplicates`를 적용하여 중복 레코드 중 1건만 남기고 제거 (전체 데이터 3,000건 확정)
* **신규 시스템 짝궁 기능:**
  * DB `tracking_no` 컬럼에 `PRIMARY KEY` 및 `UNIQUE` 제약 조건 부여
  * 접수 API 호출 시 동일 요청 재전송을 차단하는 API 멱등성(Idempotency) 보장 기능 적용

### 2) 상태(`status`) 값 파편화
* **못 믿는 값:** 띄어쓰기 오류(`배송 완료`, `미 배송`) 및 현장 임의 용어(`배달완료`, `회수`, `배송중` 등) 16가지 표기 파편화 (128건)
* **처리 방침:** 문서 기준 **7개 표준 상태**(`집화처리`, `간선상차`, `간선하차`, `배송출발`, `배송완료`, `미배송`, `반품`)로 100% 매핑 치환
* **신규 시스템 짝궁 기능:**
  * 백엔드 및 DB에 `Status Enum` 코드 작성 (자유 텍스트 저장 차단)
  * 화면상 수동 입력 금지 및 드롭다운(Select Box) 선택 방식으로 제한

### 3) 수치형 데이터(무게/치수/요금) 오류
* **못 믿는 값:** 단위 문자 혼입(96건), 음수/0 수치(37건), 극단 오타(26건), 요금 무게 계산 불일치(179건)
* **처리 방침:**
  * 원본 데이터 보존을 위한 `raw_` 컬럼 7개 신설 (`raw_weight_kg` 등)
  * 물리적 범위를 벗어난 수치($\le 0$, 무게 $> 100\text{kg}$, 치수 $> 200\text{cm}$)는 `NaN`으로 격리
  * 부피 무게($\frac{\text{가로} \times \text{세로} \times \text{높이}}{6000}$) 및 요금 무게($\max(\text{실제무게}, \text{부피무게})$) 자동 재계산
* **신규 시스템 짝궁 기능:**
  * 프론트엔드 입력 필드 `number` 제한 및 실시간 범위 검증 ($0 < \text{무게} \le 100$, $0 < \text{치수} \le 200$)
  * 부피 무게/요금 무게 수동 입력 차단 및 백엔드 자동 계산 엔진(Generated Column) 도입

### 4) 크기 등급(`size_grade`) 비표준 표기
* **못 믿는 값:** 축약 표기(`극소`, `대` 등 33건), 규격 외 표기(6건), 결측치(58건) 등 총 97건 비표준 데이터
* **처리 방침:** 수동 입력값을 배제하고, 정제된 요금 무게 기준 규정 4개 등급(`극소형`, `소형`, `중형`, `대형`)으로 일괄 재부여
* **신규 시스템 짝궁 기능:** `size_grade` 수동 선택 입력창을 제거하고 요금 무게 기반 자동 판정 로직(Rule Engine) 적용

### 5) 상태(`status`) vs 완료 시각(`delivered_at`) 모순
* **못 믿는 값:** 미완료 상태에 시각 존재(69건), 완료 상태에 시각 누락(65건) 등 총 134건 모순
* **처리 방침:** `status == '배송완료'`인 건만 완료 시각을 유지하고, 미완료 건의 시각은 `NaN`으로 초기화
* **신규 시스템 짝궁 기능:** 기사 앱에서 상태를 `배송완료`로 변경하는 순간 서버 현재 시각(`now()`)이 자동 기록되는 이벤트 트리거 구조 도입

### 6) 접수 시각 vs 완료 시각 시점 역전
* **못 믿는 값:** 접수일시보다 완료일시가 더 과거로 기록된 논리 오류 44건
* **처리 방침:** 타임스탬프 오입력 건으로 간주하여 `delivered_at`을 `NaN` 처리 후 오류 로그로 분류
* **신규 시스템 짝궁 기능:** `delivered_at >= accepted_at` 가드레일 조건 설정하여 시점 역전 데이터 입력 시 API 차단

### 7) 도착 지역(`receiver_area`) vs 권역(`region_type`) 불일치
* **못 믿는 값:** 서울/경기 등 내륙을 '제주' 권역으로 오입력하여 과다 청구 위험이 있는 20건 등 총 21건 오매핑
* **처리 방침:** 수동 선택값을 무시하고 주소/동 정보 마스터 테이블과 대조하여 `일반`, `제주`, `도서산간` 권역 100% 자동 재매핑
* **신규 시스템 짝궁 기능:** 권역 수동 선택 필드 삭제, 도로명/지번 주소 입력 시 시스템이 권역을 자동 판정

### 8) 전화번호(`receiver_phone`) 포맷 오류
* **못 믿는 값:** 맨 앞 '0' 누락(`103552703` 등) 및 번호 결측 600건
* **처리 방침:** 맨 앞 '0' 누락 건은 `010-XXXX-XXXX` 포맷으로 자동 보정, 형식 불능 건은 `NaN` 격리
* **신규 시스템 짝궁 기능:** 전화번호 정규표현식(`^010-[0-9]{4}-[0-9]{4}$`) 검증 및 UI Auto-formatting 적용

---

## 2. 검산 (만든 것을 정답과 대 봤나)

> **목적:** 원본 데이터(`shipments.csv`)와 1~3단계 정제 데이터를 수식 및 규칙 기반으로 대조하여 도출된 검산 숫자입니다.

### 📋 정제 및 검산 수치 요약표

| 검산 항목 | 대조 및 검산 내용 | 대조해서 나온 주요 숫자 |
| :--- | :--- | :--- |
| **행 수 수량 검산** | 원본 데이터에서 완전 중복 레코드 제거 검산 | **3,045행 $\rightarrow$ 3,000행** (45행 중복 삭제) |
| **상태값 매핑 검산** | 16개 파편화 텍스트의 표준 7개 상태 매핑율 검산 | **3,000건 100% 매핑 달성** (미매핑 0건) |
| **원본 보존 검산** | 수치 정제 전 원본 데이터 추적성 확보 검산 | **`raw_` 컬럼 7개 신설** (원본 수치 100% 보존) |
| **부피 무게 재계산 검산** | $\frac{\text{가로} \times \text{세로} \times \text{높이}}{6000}$ 공식 검산 | 원장 오류 및 결측 **170건 보정 완료** |
| **요금 무게 재계산 검산** | $\max(\text{실제무게}, \text{부피무게})$ 공식 검산 | 부피무게 누락 및 실제무게 오적용 **179건 보정 완료** |
| **수치 이상치 격리 검산** | 음수/0/규격 초과 이상치 수치 격리 검산 | **무게 48건 / 치수 55건 / 요금 215건 `NaN` 격리** |
| **시각 논리 교차 검산** | 완료 시각 모순 및 접수일시 대비 역전 검산 | **상태 모순 134건, 시점 역전 44건 탐지** |
| **권역 매핑 교차 검산** | 도착 지역과 권역 구분 불일치 데이터 탐지 | **내륙-제주 권역 오매핑 21건 탐지** |

---

## 3. 남기기 (다음 사람이 이어받을 수 있나)

> **목적:** 다음 작업자나 개발팀이 정제 데이터를 즉시 활용하고 신규 시스템에 반영할 수 있도록 배포 파일과 명세를 기록합니다.

### 1) 산출물 파일 배포 주소 (Data Artifacts)
* **1~3단계 정제 완료 CSV:** `shipments_step1_to_3_cleaned.csv`
  * **주요 내용:** 중복 45건 제거(3,000건), 7개 표준 상태 매핑 완료, `raw_` 원본 보존 컬럼 7개 포함, 이상치 `NaN` 격리 및 부피/요금 무게 재계산 완료 데이터셋
* **원본 데이터셋:** `shipments - 김광진.csv` (원천 로데이터 3,045건)
* **기준 참조 문서:** `03_데이터_사전.md`, `04_택배업_기본지식.md`

### 2) 신규 시스템 개발용 JSON 검증 명세 (Validation Schema Record)

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ShipmentValidationSchema",
  "type": "object",
  "properties": {
    "tracking_no": {
      "type": "STRING",
      "pattern": "^[0-9]{10}$",
      "unique": true,
      "required": true,
      "description": "운송장 번호 10자리 (Primary Key)"
    },
    "status": {
      "type": "ENUM",
      "allowed_values": ["집화처리", "간선상차", "간선하차", "배송출발", "배송완료", "미배송", "반품"],
      "required": true
    },
    "receiver_phone": {
      "type": "STRING",
      "pattern": "^010-[0-9]{4}-[0-9]{4}$",
      "required": true
    },
    "weight_kg": {
      "type": "FLOAT",
      "min": 0.01,
      "max": 100.0,
      "required": true
    },
    "dimensions": {
      "width_cm": { "type": "FLOAT", "min": 1.0, "max": 200.0, "required": true },
      "height_cm": { "type": "FLOAT", "min": 1.0, "max": 200.0, "required": true },
      "depth_cm": { "type": "FLOAT", "min": 1.0, "max": 200.0, "required": true }
    },
    "timestamps": {
      "accepted_at": { "type": "DATETIME", "required": true },
      "delivered_at": { 
        "type": "DATETIME", 
        "required": false,
        "validation": "delivered_at >= accepted_at AND status == '배송완료'"
      }
    },
    "computed_fields": {
      "volume_weight_kg": "(width_cm * height_cm * depth_cm) / 6000.0",
      "billed_weight_kg": "MAX(weight_kg, volume_weight_kg)",
      "size_grade": "IF billed_weight_kg <= 2.0 THEN '극소형' ELSE IF billed_weight_kg <= 5.0 THEN '소형' ELSE IF billed_weight_kg <= 15.0 THEN '중형' ELSE '대형'",
      "region_type": "MAP_ADDRESS_TO_REGION(receiver_area, receiver_dong)"
    }
  }
}
```